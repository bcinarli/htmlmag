/**
 * @author Bilal Cinarli
 */

module.exports = {
    default: {
        description: 'Default tasks',
        tasks      : ['browserSync', 'watch', 'sass:dist', 'jshint', 'concat', 'uglify']
    }
};